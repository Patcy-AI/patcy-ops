# The Ops Console runs fully WITHOUT any key (deterministic pipeline).
# A key is only needed for the optional conversational LangChain agent.
LLM_PROVIDER=groq
GROQ_API_KEY=gsk_your_key_here

# Production integration credentials (documented connectors — not required for the demo):
# GMAIL_OAUTH_CLIENT_ID=
# QUICKBOOKS_CLIENT_ID=
# QUICKBOOKS_CLIENT_SECRET=
